[project]
name = "patchwork-backend"
version = "0.1.0"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
markers = ["integration", "e2e"]
norecursedirs = ["tests/e2e/fixtures"]
